import { Grid } from "@mui/material";
import ProfileDisplay from "../component/display/ProfileDisplay";

const Home = () => {
    return (
        <Grid>
            <ProfileDisplay />
        </Grid>
    );
}

export default Home;